services:
  one:
    image: study
    environment:
      NAME: gamma-one
  two:
    image: study
    environment:
      NAME: gamma-two
    networks:
      default: {}
      alpha-net:
        aliases:
          - twenty

networks:
  alpha-net:
    name: alpha-net
    external: true
